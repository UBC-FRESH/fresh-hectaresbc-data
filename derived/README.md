# Derived Products

Derived products must be documented before use and must preserve source
provenance.

Current derived products:

- `web_map_previews/v1/`: source-derived browser map-preview artifacts generated
  by the main `fresh-hectaresbc` repository. Compact JSON/CSV manifests and
  validation summaries are Git-tracked. `preview.png` payloads are annexed and
  published through the configured object-store special remote.
